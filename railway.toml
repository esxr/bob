[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.railway"

[deploy]
startCommand = "node dist/src/index.js --goal 'Hello from Bob Agent on Railway!' --evaluation 'Successfully deployed'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
